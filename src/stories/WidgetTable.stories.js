import { WidgetTable } from "./WidgetTable";

export default {
  title: "Components/WidgetTable",
  argTypes: {},
};

const Template = (args) => {
  return WidgetTable(args);
};
export const Default = Template.bind({});
Default.args = {};
